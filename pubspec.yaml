name: navigation_rail_plus
description: "An enhanced NavigationRail widget for Flutter with extended customization, improved flexibility, and responsive behavior."
version: 0.0.1
homepage: https://github.com/fateme-shm/navigation_rail_plus
repository: https://github.com/fateme-shm/navigation_rail_plus
issue_tracker: https://github.com/fateme-shm/navigation_rail_plus/issues

environment:
  sdk: ^3.11.5
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  get: ^4.7.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

platforms:
  ios:
  web:
  android:
  windows:
  linux:
  macos:
